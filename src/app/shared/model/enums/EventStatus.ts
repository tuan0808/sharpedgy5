
export enum EventStatus {
    WIN = 'WIN',
    LOSS = 'LOSS',
    ACTIVE = 'ACTIVE',
    PENDING = 'PENDING',
    SCHEDULED = 'SCHEDULED',
    CANCELLED = 'CANCELLED',
    DELAYED = 'DELAYED',
    FAILED = 'FAILED',
    PUSH = 'PUSH',
}
